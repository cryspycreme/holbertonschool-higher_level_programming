The ``print_sorted`` module
=============================

Using ``print_sorted``
-------------------

Import function from module 1-my_list.py:
>>> print_sort = __import__('1-my_list').MyList.print_sorted

Tests:
    Test 1 - positive test
    >>> my_list = [1, 8, 5, 3]
    >>> print_sort(my_list)
    [1, 3, 5, 8]

    Test 2 - list contains negative number
    >>> my_list = [1, -8, 5, 3]
    >>> print_sort(my_list)
    [-8, 1, 3, 5]

    Test 3 - list contains a 0
    >>> my_list = [1, -8, 0, 3]
    >>> print_sort(my_list)
    [-8, 0, 1, 3]

    Test 4 - empty list
    >>> my_list = []
    >>> print_sort(my_list)
    []

    Test 5 - returns a new list
    >>> my_list = [1, -8, 0, 3]
    >>> print_sort(my_list)
    [-8, 0, 1, 3]
    >>> print(my_list)
    [1, -8, 0, 3]

    Test 6 - inherits from list
    >>> MyList = __import__('1-my_list').MyList
    >>> issubclass(MyList, list)
    True

    Test 7 - object instantiated
    >>> MyList = __import__('1-my_list').MyList
    >>> my_list = MyList()
    >>> isinstance(my_list, MyList)
    True

    Test 8 - prints as __str__
    >>> def __str__(self):
    ...    return [self]

    Test 9 - appends
    >>> MyList = __import__('1-my_list').MyList
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(2)
    >>> print(my_list)
    [1, 2]
